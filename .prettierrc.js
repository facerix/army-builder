module.exports = {
  // Match codebase style: single quotes (more common in codebase)
  singleQuote: true,
  // Match codebase style: semicolons (for consistency)
  semi: true,
  // Match codebase style: 2 spaces
  tabWidth: 2,
  useTabs: false,
  // Match codebase style: trailing commas (ES5 style)
  trailingComma: "es5",
  // Match codebase style: line length
  printWidth: 100,
  // Match codebase style: omit parentheses for single-param arrow functions (unit => {})
  arrowParens: "avoid",
  // Match codebase style: bracket spacing
  bracketSpacing: true,
  // Match codebase style: end of line
  endOfLine: "lf",
  // HTML/JSX formatting
  htmlWhitespaceSensitivity: "css",
  // Embedded language formatting
  embeddedLanguageFormatting: "auto",
};
